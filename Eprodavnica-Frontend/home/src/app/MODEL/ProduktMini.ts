export class ProduktMini {
    naziv: string
    cena: number
    ocena: number
    
    constructor(
        naziv: string,
        cena: number,
        ocena: number,
        
    ) {
        this.naziv = naziv
        this.cena = cena
        this.ocena = ocena
        
    }
}